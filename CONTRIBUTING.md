# Contributing

感谢参与 Nucleus Cards。提交变更前请先建立 issue 或在团队内确认范围，尤其是数据来源、医疗/伤病表述、价格计算和隐私逻辑。

## 开发流程

1. 从主分支创建短生命周期分支。
2. 复制 `.env.example` 为 `.env.local`，不要提交密钥、Token、用户数据或受限平台导出。
3. 保持演示模式无需外部账号即可运行。
4. 新指标、校验器和 API 行为需要单元测试；关键用户流程需要 Playwright 覆盖。
5. 提交前运行 `pnpm check` 与 `pnpm test:e2e`。

## 数据与内容规则

- 不绕过登录、验证码、付费墙、robots 或反自动化措施。
- 新数据源必须声明授权状态、允许的能力、原始 URL、抓取时间和错误状态。
- 在售记录、社区提交、估算和认证成交不可混用。
- 样本不足时显示区间或“暂无可信成交数据”，不可填造价格。
- AI 不得输出收益保证、确定性涨跌或医学诊断。
- 不提交未经授权的卡图、联盟标识、球员肖像或第三方品牌素材。

## 代码规范

- TypeScript 保持 strict；边界输入使用 Zod。
- 私有数据访问必须先验证当前用户，后台操作需要服务端角色校验和审计。
- 界面须支持键盘、焦点状态、移动端以及不依赖颜色的状态说明。
- 产品名、币种、时区和免责声明优先从集中配置读取；用户文案放入 i18n 结构。

当前仓库未选择开源协议。贡献代码的使用范围应由仓库所有者另行确认。
